name: Build Workspace Packages
description: Builds shared packages (types) in dependency order

runs:
  using: composite
  steps:
    - name: Build types package
      run: |
        echo "Building @packages/types..."
        pnpm --filter @packages/types build
      shell: bash
